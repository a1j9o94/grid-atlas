// The page is intentionally empty: the atlas renders from the layout so it
// survives navigation. This route exists so every deep-link path resolves;
// per-route metadata and validation land with the path router.
export default function View() {
  return null;
}
